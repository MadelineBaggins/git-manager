<config>
  <store>$HOME/git-manager</store>
  <symlinks>$HOME</symlinks>
  <repo name="admin">
    <symlink>admin</symlink>
    <post-receive>
      #!/user/bin/env/bash
      cd ..
      git --git-dir=.git reset --hard
      git-manager switch
    </post-receive>
  </repo>
</config>
