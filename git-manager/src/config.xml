<config>
  <store>$STORE</store>
  <symlinks>$SYMLINKS</symlinks>
  <repo name="admin">
    <symlink>admin</symlink>
    <post-receive>
      #!/usr/bin/env bash
      cd ..
      git --git-dir=.git reset --hard
      git-manager switch
    </post-receive>
  </repo>
</config>
